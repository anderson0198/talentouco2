<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/ivTalento"
        android:layout_width="150dp"
        android:layout_height="150dp"
        tools:background="@color/loginBkColor"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"/>
    <TextView
        android:id="@+id/tvTalentoNombre"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="Ejemplo Nombre"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toEndOf="@+id/ivTalento"/>
    <TextView
        android:id="@+id/tvTalentoCarrera"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="Ejemplo Carrera"
        app:layout_constraintTop_toBottomOf="@+id/tvTalentoNombre"
        app:layout_constraintStart_toEndOf="@+id/ivTalento"/>

</androidx.constraintlayout.widget.ConstraintLayout>